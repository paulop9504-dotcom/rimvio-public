/** Pre-pipeline routing entry — re-exports canonical resolver until probe split lands. */
export { resolveOrchestratorEarlyDecision } from "@/lib/action-chat/orchestrator/resolve-orchestrator-decision";
